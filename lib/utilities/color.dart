import 'package:flutter/material.dart';

const blue = Color.fromARGB(255, 13, 133, 232);
const blue2 = Color.fromARGB(255, 10, 53, 126);
const black2 = Color.fromARGB(255, 0, 0, 0);
final darkBlue = Colors.blueGrey[900];
const white = Color.fromARGB(255, 202, 202, 202);
final black = Colors.grey[800];
const red = Color.fromARGB(255, 161, 0, 0);
const green = Color.fromARGB(255, 16, 103, 84);
const yellow = Color.fromARGB(255, 120, 57, 5);

const teal = Color(0xFF009688); // Teal
const indigo = Color(0xFF3F51B5); // Indigo
const purple = Color(0xFF673AB7); // Purple
const pink = Color(0xFFE91E63); // Pink
const deepOrange = Color(0xFFFF5722); // Deep Orange
const amber = Color(0xFFFFC107); // Amber
const lime = Color(0xFFCDDC39); // Lime
const lightBlue = Color(0xFF03A9F4); // Light Blue
const cyan = Color(0xFF00BCD4); // Cyan
const deepPurple = Color(0xFF673AB7); // Deep Purple

const lavender = Color(0xFF9575CD); // Lavender
const mint = Color(0xFF66BB6A); // Mint
const coral = Color(0xFFF06292); // Coral
const deepGreen = Color(0xFF388E3C); // Deep Green
const amberDark = Color(0xFFFFA000); // Dark Amber
const slateGray = Color(0xFF607D8B); // Slate Gray
const maroon = Color(0xFF800000); // Maroon
const olive = Color(0xFF808000); // Olive
const steelBlue = Color(0xFF4682B4); // Steel Blue
const golden = Color(0xFFFFD700); // Golden
